AM_CFLAGS = -Wall -std=c99 -O2
AM_LDFLAGS = -lm

# this lists the binaries to produce, the (non-PHONY, binary) targets in
# the previous manual Makefile
bin_PROGRAMS = 4ipnet-automake
4ipnet_automake_SOURCES = main.c
